## 更新日志

### 2026-08-06 互动与世界重建 vNext
- 比赛互动扩展为 16 种，并按球员位置过滤；加入倒计时、动态指针、方向、线路、力度、节奏和决策输入。
- 比赛结果统一回写单一成长事务，成功、失败、跳过和超时均写入比赛历史与反馈。
- 世界地图改为洲、国家、城市、球队四级探索；加入本地 Natural Earth 国界轮廓和同城球队聚合。
- 赛季结束生成永久履历卡，记录 OVR、身价、出场、荣誉、评分、转会和伤病摘要。
- 事件加入稀有度、风险、收益、消耗、冷却、延迟事件和后续事件链。
- 修复覆盖层连续切换时旧 Sheet 残留造成的双 dialog 和滚动锁竞态。
- 本地验证：`npm test` 32/32、`npm run build`、静态/运行时/产品/玩法/动画/移动审计通过。

### 2026-08-04 仓库基线
- 确认 `index.html -> src/app.js` 为当前生产入口，`dist/` 为唯一可重建输出。
- 新增仓库卫生测试与非部署 CI，阻止构建产物、日志、压缩包和真实环境文件进入版本控制。
- 构建版本统一读取 `package.json`，不再在脚本中重复硬编码。
- 建立本地备份分支和标签；GitHub 连接恢复并完成远程验证前不合并、不部署。

### 重构
- `src/app.js`：统一应用壳层、路由、顶栏、底栏、弹窗与状态流。
- `styles.css`：建立唯一设计系统、移动端安全区、响应式卡片和动画规范。
- `src/pages/createPlayer.js`：六步沉浸式球员创建流程。
- `src/pages/career.js`：首页控制台、优先待办和紧凑两列信息架构。
- `src/pages/training.js`：训练策略、风险、浮点成长和结果结算。
- `src/pages/match.js`：赛前战术、比赛模拟、关键时间线和赛后四卡反馈。
- `src/pages/transfer.js`：世界→大洲→国家→联赛→球队的探索流程。
- `src/pages/more.js`：产品级二级入口与设置中心。

### 修复
- 时间推进选项不再共用同一逻辑。
- 暂停会停止日期、事件与结算循环，恢复不会重复处理日期。
- 伤病具备 active / returning / recovered 状态并可随时间恢复。
- 能力成长改为内部浮点，整数突破有反馈。
- 雷达图固定显示当前、上阶段与潜力三层轮廓。
- 事件加入指纹、冷却、赛季/生涯上限与最近交互限制。
- Bottom Sheet 支持遮罩、关闭按钮、下滑与 Esc 关闭。
- 移动端加入安全区、无横向溢出和 44px 最小点击区。

### 新增
- 60+ 个统一 SVG 图标入口。
- 50+ 个反馈状态目录与多种提示、爆发、警告、结果面板效果。
- 16 种事件交互形式和具体足球场景事件。
- 成长日志、伤病日志、角色记忆、转会关注与报价流程。
- Cloudflare Pages Actions 部署统一使用项目 `zuqiu`：PR 发布隔离预览，`main` 发布生产候选 `zuqiu-4tt.pages.dev`；原生 Git 自动构建、重复“项目 1”和 Worker `football` 的控制台冻结仍待完成。

### 删除
- 本压缩包为统一重构版，不包含旧版 `ux-v18.2.css`、`pace-v18.3.css`、旧模态、旧按钮和旧事件模板。

### 测试
- 运行 `npm test`
- 运行 `npm run build`
