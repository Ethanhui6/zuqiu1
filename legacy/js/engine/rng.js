export function hashSeed(...parts){const input=parts.join('|');let hash=2166136261;for(let i=0;i<input.length;i++){hash^=input.charCodeAt(i);hash=Math.imul(hash,16777619)}return hash>>>0}export function mulberry32(seed){let value=seed>>>0;return function(){value+=0x6d2b79f5;let r=value;r=Math.imul(r^(r>>>15),r|1);r^=r+Math.imul(r^(r>>>7),r|61);return((r^(r>>>14))>>>0)/4294967296}}export function pickWeighted(items,key,random){const total=items.reduce((s,x)=>s+Number(x[key]??1),0);let roll=random()*total;for(const item of items){roll-=Number(item[key]??1);if(roll<=0)return item}return items.at(-1)}export function resolveOutcome({saveId,season,eventId,option}){const seed=hashSeed(saveId,season,eventId,option.id),random=mulberry32(seed),roll=random();let cursor=0;for(const outcome of option.outcomes){cursor+=outcome.probability;if(roll<cursor)return{seed,roll,outcome}}return{seed,roll,outcome:option.outcomes.at(-1)}}