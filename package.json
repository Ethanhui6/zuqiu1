{
  "name": "football-career-simulator-v20",
  "version": "20.30.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "lint": "node tests/phase29-final-cleanup-gate.mjs --static",
    "typecheck": "node --test tests/project-contract.test.mjs tests/version-contract.test.mjs tests/world-registry.test.mjs",
    "test:repo": "node --test tests/repository-hygiene.test.mjs",
    "check": "npm run lint && npm run typecheck && npm test && npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
