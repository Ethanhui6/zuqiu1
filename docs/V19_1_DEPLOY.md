# V19.1 部署

## 静态游戏

将部署包根目录内容上传到现有 Cloudflare Pages 项目。不要把压缩包本身作为网站根文件。

## GitHub

解压源码包覆盖仓库工作区，保留 `.git`，随后提交并推送。Cloudflare Pages继续使用原有构建设置即可。

## 缓存

本版缓存名称：`green-pitch-v19.1.0-20260803-2`。激活后会清理旧缓存；HTML使用更新优先策略。调试可使用 `?no-sw=1`。
