# V18.1 测试报告

测试日期：2026-08-03  
测试版本：18.1.0  
构建编号：2026-08-03.4

## 自动化命令

```bash
npm test
```

所有命令通过。

## 数据与静态检查

- 俱乐部记录：500，ID与中文名称均唯一
- 成长模板记录：500，ID唯一；包含100个不同模板名称，每个位置50条变体
- 成就：330，ID唯一；隐藏成就50
- 事件记录：1,200，ID唯一
- 不同事件标题：719
- 原始候选选择：6,000
- 不重复选择文字：3,215
- 所有JSON可解析
- 所有相对模块导入有效
- 关键源码未发现 `Math.random()`
- Service Worker缓存版本与预缓存文件检查通过

## 核心和验收测试

通过：

- 不同位置球员创建和低概率传奇天赋
- 青年队条件晋升、长期未首发和名单身份
- 高负荷训练伤病、医疗资金不足和完整康复
- 比赛固定结果、关键选择、零封、时间线和未进名单分支
- 事件2—5项展示、70次连续记忆、冷却、最近12次标题防重复和选择结构防重复
- 转会生成不自动换队、拒绝、暂缓、谈判上限、租借、过期、留队锁定和主动意向
- 合同到期、续约、赛季奖项、成就不重复和多结局
- 设施互动真实写入能力、粉丝、现金、关系、历史和冷却
- 三槽存档、同步自动保存、损坏恢复、无校验旧档签名、导入校验和迁移
- 同页面刷新保持滚动位置并取消重复进场动画
- 500队重复筛选性能

## 结果汇总

- 基础系统测试：通过
- 连续24阶段生涯测试：通过，24个事件均不同
- 完整职业生涯测试：通过，180阶段、19赛季、35岁退役并生成“一人一城”结局
- 验收测试：32项通过
- 运行审计：99项通过
- 静态检查：320个文件、46个JavaScript文件通过

## 静态HTTP冒烟测试

本地静态服务器可正常返回：

- `index.html`
- `src/main.js`
- `data/version.json`
- 事件分类JSON

## 浏览器与移动端说明

代码层已验证同页刷新保留滚动、训练不整页刷新、Sheet锁定背景、分页和安全区域样式。当前执行环境的Chromium受到组织策略限制，会拦截 `localhost`、容器私网IP和 `file:` 地址，因此无法完成真实浏览器截图与触控回归。

没有把这一项写成“已通过”。部署后仍需在实际iPhone、Android和Safari中进行最终手动回归，重点观察横向溢出、键盘弹起、底部安全区域和长时间游玩内存表现。
