# 当前 UI 审计

## 已确认

- `index.html` 实际加载 `src/app.js` 和根目录 `styles.css`。
- 仓库同时保留旧页面、组件和样式树，存在双入口和冲突样式风险。
- 当前自动化测试覆盖最小点击区域和移动端安全区，但尚未覆盖全部目标视口的真实浏览器回归。

## 后续验证顺序

1. 追踪活跃入口的页面、弹层、雷达图和训练调用链。
2. 在 320、390 和 1440 宽度验证溢出、遮挡和关闭行为。
3. 只删除经全局引用、构建和行为测试共同证明无用的旧实现。
